# Public browser config. These are bundled into the website and are not secrets.
VITE_SUPABASE_URL=https://xyz.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Local-only operations. Do not expose these to the browser.
SUPABASE_DB_URL=
GROUP_FORMATION_ADMIN_TOKEN=

# Optional Supabase Edge Function secret for temporary testing only.
# Leave unset or false for normal use.
GROUP_FORMATION_DISABLE_RATE_LIMITS=false
